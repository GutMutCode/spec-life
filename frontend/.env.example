# ============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env.local and fill in your own values
# Never commit .env.local to version control (it's in .gitignore)

# ============================================================================
# SENTRY ERROR TRACKING
# ============================================================================
# Sentry is used for error monitoring and performance tracking in production.
# To enable Sentry:
# 1. Create a free account at https://sentry.io
# 2. Create a new React project in Sentry
# 3. Copy the DSN from Project Settings → Client Keys (DSN)
# 4. Set VITE_SENTRY_DSN to your project's DSN

# Sentry DSN (Data Source Name)
# Required for error reporting to work
# Example: https://abc123def456@o123456.ingest.sentry.io/7891011
# Leave empty to disable Sentry (default for local development)
VITE_SENTRY_DSN=

# Sentry Environment
# Used to distinguish errors from different deployment environments
# Common values: development, staging, production
# Default: Uses Vite's MODE (development/production)
VITE_SENTRY_ENVIRONMENT=development

# Application Version
# Used for release tracking in Sentry
# Helps identify which version introduced bugs
# Default: 'unknown'
# Recommended: Use git commit hash or semantic version (e.g., 1.2.3)
VITE_APP_VERSION=0.1.0

# ============================================================================
# API CONFIGURATION
# ============================================================================
# Backend API endpoint
# Default: http://localhost:3000 (for local development)
# Production: Set to your deployed backend URL
VITE_API_URL=http://localhost:3000

# ============================================================================
# FEATURE FLAGS
# ============================================================================
# Enable/disable features for testing or gradual rollout

# Enable cloud sync (when implemented)
# Default: false
# VITE_ENABLE_CLOUD_SYNC=false

# Enable debug mode (additional console logs, dev tools)
# Default: false (automatically enabled in development mode)
# VITE_DEBUG_MODE=false
